# Herdr 功能对照

针对 Windows 主机。社区插件来自 `herdr.dev/plugins`（GitHub topic `herdr-plugin`）。插件不经官方审查，安装前读 manifest。

---

## 1. RemoteControl

**目标：** 局域网内 iOS / Android App 与 PC 上的 Herdr 配对。

**现状：** 没有同时满足「局域网 + 双端原生 App + Windows 主机」的现成方案。

| 方案 | 适用 | 缺口 |
|---|---|---|
| [herdr-connect](https://github.com/Tomyail/herdr-connect) | LAN、QR 配对、iOS TestFlight、Windows daemon | 无 Android |
| [herdr-remote](https://github.com/dcolinmorgan/herdr-remote) | Windows relay + 托盘，手机浏览器 / Telegram | 非 LAN 配对，非原生 App |
| [Collie](https://github.com/AltanS/collie) | iOS/Android PWA | 走 Tailscale；Windows 仅实验 |
| [Heeler](https://github.com/ZingerLittleBee/Heeler) | 原生 iOS，SSH + QR | 无 Android |
| 官方 | 手机 SSH 再跑 `herdr` | 无 App |

不要用 `0cv/herdr-mobile-relay`、`benkraus/herdr-mobile`：Windows 主机不支持。

**处理：**

- 先能控：装 `herdr-remote`。
- 坚持局域网、先用 iPhone：装 `herdr-connect`。
- 必须双端原生 + LAN：fork `herdr-connect` 补 Android，不要另起一套 relay。

---

## 2. Proxy

**目标：** AgentSession 默认走本机 Clash Verge mixed 端口 `127.0.0.1:7897`，shell 用 PowerShell 7。

**现状：** Herdr 0.8.2 没有 `[proxy]` 配置项。新 pane 会注入 `HERDR_ENV=1`（官方默认，见 [Configuration](https://herdr.dev/docs/configuration/#environment-variables)）。已在跑的进程无法热改环境。Grok / Agy 不一定认 `HTTP_PROXY`，本机已有 `grokp` / `agyp`（proxinjector）。

**已落地（不是插件）：**

1. `%APPDATA%\herdr\config.toml` 把默认 shell 设成本机 pwsh 7：

```toml
[terminal]
default_shell = "C:/Program Files/PowerShell/7/pwsh.exe"
```

2. PowerShell 7 profile（`Documents\PowerShell\Microsoft.PowerShell_profile.ps1`）在 `HERDR_ENV=1` 时写入代理，Herdr 外面的 pwsh 不走这段：

```powershell
if ($env:HERDR_ENV -eq "1") {
    if (-not $env:HTTP_PROXY) { $env:HTTP_PROXY = "http://127.0.0.1:7897" }
    if (-not $env:HTTPS_PROXY) { $env:HTTPS_PROXY = "http://127.0.0.1:7897" }
    if (-not $env:ALL_PROXY) { $env:ALL_PROXY = "http://127.0.0.1:7897" }
    if (-not $env:NO_PROXY) { $env:NO_PROXY = "localhost,127.0.0.1,::1" }
}
```

3. 当前 Windows 用户环境也写了同样的 `HTTP_PROXY` / `HTTPS_PROXY` / `ALL_PROXY` / `NO_PROXY`，给「不经过 pwsh、由 Herdr 直接拉起的进程」用。

**验证（必须新 pane，旧 pane 仍是改之前的环境）：**

```powershell
$env:HERDR_ENV          # 应为 1
$env:HTTP_PROXY         # http://127.0.0.1:7897
$env:HTTPS_PROXY
$env:ALL_PROXY
$env:NO_PROXY           # localhost,127.0.0.1,::1
$PSVersionTable.PSVersion   # 7.x
```

Grok / Agy 继续用 `grokp` / `agyp`。直接敲 `grok` / `agy` 环境变量未必生效。

---

## 3. FileExplorer / GitStatus（仅非 P4 workspace）

**目标：** Git 工作区：目录树、预览、GitDiff、提交推送；空消息时用可配置 AgentCLI oneshot 生成 commit message。

**SCM 互斥（和 §7 共用）：** 一个 Herdr workspace 只认一种 SCM 宿主。Herdr v1 没有插件依赖、也不能 per-workspace 禁用插件，互斥只做在「自动打开 / 恢复」，两套都可装着。

判定看 **当前 workspace cwd**，不是「这台机器装了 p4」：

1. cwd 在当前 client view 内（`p4 where` 能映射），且在 Client root 下或向上有 `.p4config` → **P4**，走 `herdr-perforce`（Explorer + Diff + Submit 全在该插件内）。
2. 有 `.git` 且不满足上一条 → **Git**，走社区 sidebar。
3. 两边都像 → **P4 赢**，不要自动开 Git sidebar。

不要做：P4 运行时依赖 Explorer 插件；往 sidebar 里接 P4；抽公共 Explorer 插件给两边 import。代码两套分开写，UI 可以借鉴，crate 以后再说。

**Git 侧处理：**

```
herdr plugin install alexarthurs/herdr-sidebar/plugins/herdr-sidebar
```

[herdr-sidebar](https://github.com/alexarthurs/herdr-sidebar)：树、预览、diff、stage/commit/push、Windows 可用。

小改造（不要把 sidebar 改成双 SCM）：

1. **互斥（必做，尽量不 fork）：** 关掉 sidebar 的 Auto-open。用 `herdr-perforce`（或一个极小 dispatcher）听 `workspace.focused`：P4 → 开/恢复 Perforce pane；Git → `herdr plugin action invoke herdr-sidebar.open-sidebar-windows`。
2. **Commit message（建议，可后做）：** sparkle 写死 `claude`。要对齐 P4 的 ADR-0004 oneshot，小 fork / PR sidebar 这一处即可。不要在 P4 插件里给 Git 生成 message。

不要借 sidebar 的 Git index/stage 语义、worktree 假设。只读备选（`smarzban/herdr-file-viewer` 等）不当主力。

---

## 4. 指令缓存

**目标：** 全局 + Workspace 两层保存常用 terminal 指令，调用时新开 terminal 执行一次。

**现状：** 没有同名插件。分层保存已有，执行模型差一点。

**处理：**

- 能接受「fuzzy 选中就跑」：装 [herdr-plus](https://github.com/cloudmanic/herdr-plus)。全局在插件 config 的 `quick-actions/`，仓库在 `.herdr-plus/quick-actions/`。Windows 安装需要本机 Go。
- 必须「新开 pane 看输出」：小插件。存储复用 herdr-plus 的两层 toml，执行改成 `herdr pane split` / `plugin pane open`。
- 少量固定快捷键可用原生配置，没有库的概念：

```toml
[[keys.command]]
key = "prefix+alt+b"
type = "pane"
command = "npm run build"
```

command palette、herdr-prompts 是搜已有 action / 给 Agent 的 prompt，不是这件事。

---

## 5. Browser

**目标：** Herdr 内置浏览器。

**现状：** 社区标准是 [terminal-browser](https://github.com/zenbu-labs/terminal-browser)（官方已弃用自己的 `herdr-browser`，改推这个）。仅 macOS/Linux，依赖 Kitty graphics。Windows 没有能直接装的方案。

**处理：**

- Windows 上先用系统浏览器打开 Agent 打出的 localhost。
- 若要内嵌：做薄宿主（本机 Chrome/Edge + CDP，pane 里只做地址栏/截图/控制），不要移植整页像素进 terminal。
- 完整 terminal-browser 的 Windows 移植工作量大，不优先。

---

## 6. Plannotator / Reviewr（Notator）

**目标：** 在文件/diff 上标注，发给当前 Agent 当上下文。

**已落地（Windows）：** 系统浏览器里的 [Plannotator OSS](https://docs.plannotator.ai/open-source/) 审 diff/文档（含 Perforce changelist）；Herdr 里用 [herdr-annotate](https://github.com/plannotator/herdr-annotate) 标终端选中文本。安装、快捷键、以及「批注如何回到对应 Agent Session」见 **[docs/notator.md](docs/notator.md)**。

不要用 `herdr-plannotator` / `herdr-browser` / `terminal-browser`：依赖 Kitty graphics，Windows Terminal 不可用（见 §5）。

**Git 审阅附件（未采用）：[herdr-reviewr](https://github.com/persiyanov/herdr-reviewr)**（~525★，MIT）

- 人看 Agent 的 diff，选行/`c` 写备注，`s` 发给当前 Agent；不改 worktree。
- **无 Windows**（macOS/Linux + truecolor）。Clipboard 依赖 `pbcopy`/`wl-copy`/`xclip`。
- 绑 Git worktree，P4 workspace 不能 fork。

不要用 reviewr 替换 sidebar。Git 宿主仍是 sidebar；网页批阅走 Plannotator，不走 reviewr。

**处理（现行）：**

1. Git workspace：sidebar 负责树/提交；批阅用 Plannotator（Agent 对话里开 `plannotator review`）。
2. P4 workspace：树 / Diff / Submit 仍在 `herdr-perforce`；批阅同样用 Plannotator（cwd 必须在 client view 内）。不要 fork reviewr。
3. 终端选中文本：herdr-annotate（`prefix+a`）。不是源文件行。
4. [herdr-file-annotator](https://github.com/JonasBaeumer/herdr-file-annotator) 是 MCP blocking 审阅，无 Windows，不用。

---

## 7. Perforce（P4 workspace 的整套宿主）

**现状：** 社区没有实现。继续 `D:\Projects\herdr-perforce`。

**产品边界：** P4 workspace 里不依赖社区 File/Git 插件。一个 plugin、两个内部 view：**Explorer**（client 本地树 + 预览 + 只读 opened/have 装饰）| **Review**（左 Diff、右 CL 树 + Submit）。不是 P4V；Explorer 根是 workspace cwd，不得出 Client root / view。树上不做 `p4 add/edit/delete/sync`。

**设计稿缺口（已补）：** 原 `docs/design.md` 的「文件树」只是 changelist → opened 文件，宿主图还把 `Files` 画成旁边的其他工具。工作区目录树此前没有独立章节。现已写入 §4.4 / §5.4–5.5、[ADR-0005](D:\Projects\herdr-perforce\docs\adr\0005-in-plugin-file-explorer.md)、验收 ACC-EXPLORER。实现仍待做。

**打开 / 恢复：**

- cwd 满足 §3 的 P4 判定 → startup 幂等恢复 pane（已有 remembered workspace）。
- 非 P4 → 跳过 restore；手动 Open 时明确失败（不在 client view）。
- 不卸载 sidebar，也不调它的内部 API。

**Agent 描述：** 可配置 AgentCLI oneshot，只读用户配置目录（ADR-0004）。Git commit message 不做到这个插件里；形状可以抄，配置文件可以长得像。

**实现：** Explorer / Diff / Submit 都在本插件内各写一遍，可借鉴 sidebar 的树和预览交互。不要 fork sidebar 拆 Git 插 P4。提交安全模型维持 ADR-0002/0003，不要和 Git commit 合成一个 SourceControl 插件。

---

## 落地顺序

1. **已落地：** 代理（pwsh 7 + `HERDR_ENV` profile + 用户环境变量）。
2. **已落地：** Notator（Plannotator + herdr-annotate）。配置见 [docs/notator.md](docs/notator.md)。
3. **P4：** 按 ADR-0005 实现 Explorer view；`workspace.focused` 分发（P4 开自己、Git 去调 sidebar）。检测用 client view。批阅已用 Plannotator，不必等插件内标注。
4. **Git：** 装 sidebar，关 Auto-open；批阅用 Plannotator，不移植 reviewr。可配置 commit message 再小 fork/PR sparkle。
5. **其余现成：** herdr-plus、herdr-remote（或 herdr-connect）。
6. **仍缺：** 内置浏览器、LAN 双端原生 RemoteControl。
