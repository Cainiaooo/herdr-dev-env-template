# Herdr 功能对照

针对 Windows 主机。社区插件来自 `herdr.dev/plugins`（GitHub topic `herdr-plugin`）。插件不经官方审查，安装前读 manifest。

---

## 1. RemoteControl

**目标：** 局域网内 iOS / Android 与 PC 上的 Herdr 配对并控制。

**现状：** 没有同时满足「Windows 主机 + 局域网或 Tailscale + 双端原生 App + 可接受的安全模型」的现成方案。2026-08-29 试过 pocket / herdr-remote，均已卸载。**暂不落地。** 调研与试用记录见 **[docs/remote-control.md](docs/remote-control.md)**。

| 方案 | 适用 | 结论 |
|---|---|---|
| [herdr-pocket](https://github.com/mattbidinger/herdr-pocket) | Windows 主战场、默认 LAN、PWA | **已试、已卸。** 网页 QR 是 ASCII `<pre>`，手机相机扫不出。 |
| [herdr-remote](https://github.com/dcolinmorgan/herdr-remote) | Windows relay + 手机浏览器 / Telegram | **已试、功能可用、已卸。** 一把共享 token + 明文 HTTP/`ws://`；公司大局域网不安心。 |
| [herdr-connect](https://github.com/Tomyail/herdr-connect) | LAN、真 QR、iOS TestFlight、Windows daemon | 未装。每设备 token + TLS pin，最接近 Orca 配对；无 Android；不是 Herdr 插件。 |
| [Collie](https://github.com/AltanS/collie) | iOS/Android PWA，默认 Tailscale Serve | 未装。安全模型正规；官方 Linux/macOS，Windows 仅实验。 |
| [Heeler](https://github.com/ZingerLittleBee/Heeler) | 原生 iOS，SSH + QR | 未装。无 Android；Windows 需 OpenSSH。 |
| 官方 | 手机 SSH 再跑 `herdr` | 无 App。 |
| Orca Companion | 对照：桌面+手机同一 Runtime | **不是 Herdr 插件。** 真 QR、每设备 token、TLS pin、E2EE、未配对只听 loopback。 |

不要用 `0cv/herdr-mobile-relay`、`benkraus/herdr-mobile`：Windows 主机不支持。不要叠装两套遥控面。

**处理：** 暂停。社区方案要么不好用（扫码/Windows），要么不安心（明文共享 token）。再启动时先读 [docs/remote-control.md](docs/remote-control.md)，不要默认再绑 `0.0.0.0`。

---

## 2. Proxy

**目标：** AgentSession 默认走本机 Clash Verge mixed 端口 `127.0.0.1:7897`，shell 用 PowerShell 7。

**现状：** Herdr 0.8.2 没有 `[proxy]` 配置项。新 pane 会注入 `HERDR_ENV=1`（官方默认，见 [Configuration](https://herdr.dev/docs/configuration/#environment-variables)）。已在跑的进程无法热改环境。Grok / Agy 不一定认 `HTTP_PROXY`，本机已有 `grokp` / `agyp`（proxinjector）。

**已落地（不是插件）：**

1. `%APPDATA%\herdr\config.toml` 把默认 shell 设成本机 pwsh 7：

```toml
[terminal]
default_shell = "C:/Program Files/PowerShell/7/pwsh.exe"
```

2. PowerShell 7 profile（`Documents\PowerShell\Microsoft.PowerShell_profile.ps1`）在 `HERDR_ENV=1` 时写入代理，Herdr 外面的 pwsh 不走这段：

```powershell
if ($env:HERDR_ENV -eq "1") {
    if (-not $env:HTTP_PROXY) { $env:HTTP_PROXY = "http://127.0.0.1:7897" }
    if (-not $env:HTTPS_PROXY) { $env:HTTPS_PROXY = "http://127.0.0.1:7897" }
    if (-not $env:ALL_PROXY) { $env:ALL_PROXY = "http://127.0.0.1:7897" }
    if (-not $env:NO_PROXY) { $env:NO_PROXY = "localhost,127.0.0.1,::1" }
}
```

3. 当前 Windows 用户环境也写了同样的 `HTTP_PROXY` / `HTTPS_PROXY` / `ALL_PROXY` / `NO_PROXY`，给「不经过 pwsh、由 Herdr 直接拉起的进程」用。

**验证（必须新 pane，旧 pane 仍是改之前的环境）：**

```powershell
$env:HERDR_ENV          # 应为 1
$env:HTTP_PROXY         # http://127.0.0.1:7897
$env:HTTPS_PROXY
$env:ALL_PROXY
$env:NO_PROXY           # localhost,127.0.0.1,::1
$PSVersionTable.PSVersion   # 7.x
```

Grok / Agy 继续用 `grokp` / `agyp`。直接敲 `grok` / `agy` 环境变量未必生效。

---

## 3. FileExplorer / GitStatus（仅非 P4 workspace）

**目标：** Git 工作区：目录树、预览、GitDiff、提交推送；空消息时用可配置 AgentCLI oneshot 生成 commit message。

**SCM 互斥（和 §7 共用）：** 一个 Herdr workspace 只认一种 SCM 宿主。Herdr v1 没有插件依赖、也不能 per-workspace 禁用插件，互斥只做在「自动打开 / 恢复」，两套都可装着。

判定看 **当前 workspace cwd**，不是「这台机器装了 p4」：

1. cwd 在当前 client view 内（`p4 where` 能映射），且在 Client root 下或向上有 `.p4config` → **P4**，走 `herdr-perforce`（Explorer + Diff + Submit 全在该插件内）。
2. 有 `.git` 且不满足上一条 → **Git**，走社区 sidebar。
3. 两边都像 → **P4 赢**，不要自动开 Git sidebar。

不要做：P4 运行时依赖 Explorer 插件；往 sidebar 里接 P4；抽公共 Explorer 插件给两边 import。代码两套分开写，UI 可以借鉴，crate 以后再说。

**Git 侧处理：**

```
herdr plugin install alexarthurs/herdr-sidebar/plugins/herdr-sidebar
```

[herdr-sidebar](https://github.com/alexarthurs/herdr-sidebar)：树、预览、diff、stage/commit/push、Windows 可用。

小改造（不要把 sidebar 改成双 SCM）：

1. **互斥（必做，尽量不 fork）：** 关掉 sidebar 的 Auto-open。用 `herdr-perforce`（或一个极小 dispatcher）听 `workspace.focused`：P4 → 开/恢复 Perforce pane；Git → `herdr plugin action invoke herdr-sidebar.open-sidebar-windows`。
2. **Commit message（建议，可后做）：** sparkle 写死 `claude`。要对齐 P4 的 ADR-0004 oneshot，小 fork / PR sidebar 这一处即可。不要在 P4 插件里给 Git 生成 message。

不要借 sidebar 的 Git index/stage 语义、worktree 假设。只读备选（`smarzban/herdr-file-viewer` 等）不当主力。

---

## 4. 指令缓存

**目标：** 全局 + Workspace 两层保存常用 terminal 指令，快捷键唤出列表后一键跑。

**已落地：** [herdr-plus](https://github.com/cloudmanic/herdr-plus) 0.1.24。Go 1.27.0 在 `D:\DevelopmentEnvironment\go`。装法、Windows action id、DevDocs 命令见 **[docs/herdr-plus.md](docs/herdr-plus.md)**。

| 能力 | 本机 |
|---|---|
| Quick Actions | `prefix+down` → `cloudmanic.herdr-plus.quick-actions-windows`。全局 `quick-actions/`，工程 `.herdr-plus/quick-actions/`（pane cwd 必须是工程根）。 |
| Projects | `prefix+up` → `projects-windows`。一份 TOML 起整套 workspace。 |
| 要盯输出的命令 | 上游 overlay 跑完即关，且 `[ui.toast]` 默认 off。DevDocs 用 wrapper `pane split` 到下方新 pane，看完 Enter 关。 |
| 原生 `[[keys.command]]` | 少量固定快捷键仍可用，没有库。 |

不要叠装 cmd-marks / command-center / palette 自定义命令。command palette 搜的是已有 action，不是存 Shell 命令。Windows 必须绑 `*-windows` 后缀的 action id。

---

## 5. Browser

**目标：** Herdr 内置浏览器。

**现状：** 社区标准是 [terminal-browser](https://github.com/zenbu-labs/terminal-browser)（官方已弃用自己的 `herdr-browser`，改推这个）。仅 macOS/Linux，依赖 Kitty graphics。Windows 没有能直接装的方案。

**处理：**

- Windows 上先用系统浏览器打开 Agent 打出的 localhost。
- 若要内嵌：做薄宿主（本机 Chrome/Edge + CDP，pane 里只做地址栏/截图/控制），不要移植整页像素进 terminal。
- 完整 terminal-browser 的 Windows 移植工作量大，不优先。

---

## 6. Plannotator / Reviewr（Notator）

**目标：** 在文件/diff 上标注，发给当前 Agent 当上下文。

**已落地（Windows）：** 系统浏览器里的 [Plannotator OSS](https://docs.plannotator.ai/open-source/) 审 diff / URL / 本地网页（含 Perforce changelist）；Herdr 里用 [herdr-annotate](https://github.com/plannotator/herdr-annotate) 标终端选中文本；Markdown / 计划用 [plannotator-tui](https://github.com/plannotator/plannotator-tui)（官方 Windows 包 + 本仓库 `plugins/plannotator-tui` 垫片）。安装、快捷键、以及「批注如何回到对应 Agent Session」见 **[docs/notator.md](docs/notator.md)**。

不要用 `herdr-plannotator` / `herdr-browser` / `terminal-browser`：依赖 Kitty graphics，Windows Terminal 不可用（见 §5）。

**Git 审阅附件（未采用）：[herdr-reviewr](https://github.com/persiyanov/herdr-reviewr)**（~525★，MIT）

- 人看 Agent 的 diff，选行/`c` 写备注，`s` 发给当前 Agent；不改 worktree。
- **无 Windows**（macOS/Linux + truecolor）。Clipboard 依赖 `pbcopy`/`wl-copy`/`xclip`。
- 绑 Git worktree，P4 workspace 不能 fork。

不要用 reviewr 替换 sidebar。Git 宿主仍是 sidebar；网页批阅走 Plannotator，不走 reviewr。

**处理（现行）：**

1. Git workspace：sidebar 负责树/提交；批阅用 Plannotator（Agent 对话里开 `plannotator review`）。
2. P4 workspace：树 / Diff / Submit 仍在 `herdr-perforce`；批阅同样用 Plannotator（cwd 必须在 client view 内）。不要 fork reviewr。
3. 终端选中文本：herdr-annotate（`prefix+a`）。不是源文件行。
4. Markdown / 计划 / Agent 刚说的那段：plannotator-tui（`prefix+o` / `prefix+shift+o`，或 Agent 跑 `plannotator-tui herdr open`）。不要用 Chrome 版替代终端审 Markdown 的这条路径，也不要用 TUI 审 Git/P4 diff。
5. [herdr-file-annotator](https://github.com/JonasBaeumer/herdr-file-annotator) 是 MCP blocking 审阅，无 Windows，不用。

---

## 7. Perforce（P4 workspace 的整套宿主）

**现状：** 社区没有实现。继续 `D:\Projects\herdr-perforce`。

**产品边界：** P4 workspace 里不依赖社区 File/Git 插件。一个 plugin、两个内部 view：**Explorer**（client 本地树 + 预览 + 只读 opened/have 装饰）| **Review**（左 Diff、右 CL 树 + Submit）。不是 P4V；Explorer 根是 workspace cwd，不得出 Client root / view。树上不做 `p4 add/edit/delete/sync`。

**设计稿缺口（已补）：** 原 `docs/design.md` 的「文件树」只是 changelist → opened 文件，宿主图还把 `Files` 画成旁边的其他工具。工作区目录树此前没有独立章节。现已写入 §4.4 / §5.4–5.5、[ADR-0005](D:\Projects\herdr-perforce\docs\adr\0005-in-plugin-file-explorer.md)、验收 ACC-EXPLORER。实现仍待做。

**打开 / 恢复：**

- cwd 满足 §3 的 P4 判定 → startup 幂等恢复 pane（已有 remembered workspace）。
- 非 P4 → 跳过 restore；手动 Open 时明确失败（不在 client view）。
- 不卸载 sidebar，也不调它的内部 API。

**Agent 描述：** 可配置 AgentCLI oneshot，只读用户配置目录（ADR-0004）。Git commit message 不做到这个插件里；形状可以抄，配置文件可以长得像。

**实现：** Explorer / Diff / Submit 都在本插件内各写一遍，可借鉴 sidebar 的树和预览交互。不要 fork sidebar 拆 Git 插 P4。提交安全模型维持 ADR-0002/0003，不要和 Git commit 合成一个 SourceControl 插件。

---

## 8. Tab auto-rename

**目标：** Herdr 内部 tab 跟着当前工作可读，不再长期停在 `1` / `2` / `3`。一个 tab 里多个终端时，用该 tab 内焦点那路 agent 的名字。手工改过的 tab 不要被覆盖。

**现状：** Herdr 会捕获 pane 的 OSC 标题（`terminal_title_stripped`），但 tab 栏默认仍是数字。官方不打算内建 OSC→tab（discussion [#578](https://github.com/herdrdev/herdr/discussions/578)）。社区分两路：再开一个模型起名，或直接复用 CLI 已经写到终端标题里的字。本机走后者。

| 方案 | 适用 | 缺口 |
|---|---|---|
| [herdr-pane-topic-sync](https://github.com/danbuhler/herdr-pane-topic-sync) | Windows、Bun、OSC→pane/tab；可 `tab_source = "active"` | `{topic}` 就是 OSC 原文。Grok 会把 responding / waiting 写进标题，插件不剥。 |
| [herdr-tab-smart-rename](https://github.com/iurysza/herdr-tab-smart-rename) | Windows、任务级启发式 + 可选 LLM | 另起名字，默认 OpenAI；不读 Grok/Agy session 文件。 |
| [herdr-tab-renamer](https://github.com/ryanlewis/herdr-tab-renamer) | 同样吃 OSC | 无 Windows。 |
| [herdr-tab-session-name-sync](https://github.com/itayo-m/herdr-tab-session-name-sync) | Windows、焦点定 tab | 默认只管 Copilot / OpenCode。 |

不要叠装第二套 rename 插件。不要用 `jovylle/herdr-session-title-name`：清单写了 Windows，启动命令是 `sh`。

**已落地：** `danbuhler/herdr-pane-topic-sync`，配置见 **[docs/tab-rename.md](docs/tab-rename.md)**。

---

## 9. Workspace 过往 Agent Session

**目标：** 在终端里快速列出当前 Herdr workspace（按项目 cwd）跑过哪些 Agent Session，并能翻对话、必要时 resume。不是 Herdr 自己的 workspace/tab/pane 树，也不是当前还开着的 agent pane。

**现状：** 社区有几家在做「inbox / 归档搜索 / 跨环境挖掘」，功能对得上，但 **没有 Windows 面**；inbox / omnisearch 还不读 Grok。Herdr 本体没有对话浏览器。本机 Grok 已按 cwd 存 session。**暂不装 Herdr 插件。** 调研见 **[docs/agent-session-history.md](docs/agent-session-history.md)**。

| 方案 | 适用 | 结论 |
|---|---|---|
| [herdr-agent-inbox](https://github.com/douglascorrea/herdr-agent-inbox) | `prefix+i` inbox，`h` 按 workspace 列已关闭会话并可 resume | **不装。** 只读 Claude / Pi / Codex transcript，不读 Grok。`platforms` linux/macos，`/bin/sh`。 |
| [herdr-omnisearch](https://github.com/dmnkf/herdr-omnisearch) | `prefix+shift+o` 搜归档对话并 resume | **不装。** 默认归档只配 Codex + Claude。无 Windows。 |
| [session-digger](https://github.com/taxueseek/session-digger) | `sd-sessions` / `sd-search`；明确支持 Grok `chat_history.jsonl` | **不装。** 能力最贴本环境，清单仍是 linux/macos，动作走 `bash`。 |
| [herdr-vaultr](https://github.com/connerohnesorge/herdr-vaultr) / [herdr-catchup](https://github.com/wilbeibi/herdr-catchup) | 当前 pane 的那条会话：看 transcript / 交接 | 不是按 workspace 列历史。无 Windows。 |
| [herdr-session-parker](https://github.com/iviaxpow3r/herdr-session-parker) | 停掉正在跑的 pane，以后 resume | 不是浏览过往对话。无 Windows。 |
| [herdr-plugin-sesh](https://github.com/fullerzz/herdr-plugin-sesh) / [herdr-recent-workspaces](https://github.com/ismaelosuna7824/herdr-recent-workspaces) | 挑 Herdr workspace / 最近文件夹 | 名字里的 session 是 workspace，不是 Agent 对话。 |
| [herdr-insight](https://github.com/0x5c0f/herdr-insight) / [herdr-nav-history](https://github.com/jugyo/herdr-nav-history) | Agent 状态时间线；pane 焦点前进后退 | 不是对话内容。 |
| 官方 `prefix+g` / `[session] resume_agents_on_restore` | workspace/tab/pane 树；重启后续上当时那个 session | 没有过往对话列表。 |
| Grok `/resume`、`grok sessions list` | 当前 cwd 的 session 列表、搜内容、resume | **现行办法。** 不经过 Herdr 插件。 |

不要为了「看起来能列 session」去装 inbox / omnisearch / session-digger：Windows 装不上，inbox 也看不到 Grok。不要和 §8 tab 命名叠：tab-rename 不读 session 文件。

**处理：** 暂停 Herdr 侧。当前 workspace 的过往 Grok session 用 `/resume` 或 `grok sessions list`。以后要做成 Herdr 弹层，要么自己写 Windows 插件，要么 fork session-digger 补 PowerShell 启动面；现成的不能装。

---

## 落地顺序

1. **已落地：** 代理（pwsh 7 + `HERDR_ENV` profile + 用户环境变量）。
2. **已落地：** Notator（Plannotator + herdr-annotate）。配置见 [docs/notator.md](docs/notator.md)。
3. **已落地：** Tab auto-rename（pane-topic-sync，OSC→tab，焦点定名）。配置见 [docs/tab-rename.md](docs/tab-rename.md)。
4. **已落地：** herdr-plus（Quick Actions + Projects；DevDocs publish/sync-all）。配置见 [docs/herdr-plus.md](docs/herdr-plus.md)。
5. **P4：** 按 ADR-0005 实现 Explorer view；`workspace.focused` 分发（P4 开自己、Git 去调 sidebar）。检测用 client view。批阅已用 Plannotator，不必等插件内标注。
6. **Git：** 装 sidebar，关 Auto-open；批阅用 Plannotator，不移植 reviewr。可配置 commit message 再小 fork/PR sparkle。
7. **仍缺 / 暂停：** 内置浏览器。RemoteControl 已调研并试用，暂不装，见 §1 与 [docs/remote-control.md](docs/remote-control.md)。Workspace 过往 Agent Session 已调研，暂不装 Herdr 插件，见 §9 与 [docs/agent-session-history.md](docs/agent-session-history.md)；先用 Grok `/resume` / `grok sessions list`。
